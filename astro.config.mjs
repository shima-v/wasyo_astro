import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://www.wwwasyo.com', // 取得したドメイン
  base: '/',                       // サブディレクトリ（wasyo_astro）を空にする
});